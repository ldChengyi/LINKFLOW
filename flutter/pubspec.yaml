name: linkflow_app
description: LinkFlow Flutter 语音助手 APP
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <5.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  speech_to_text: ^7.0.0
  just_audio: ^0.10.0
  shared_preferences: ^2.2.0
  provider: ^6.1.0
  sqflite: ^2.3.0
  path: ^1.8.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
